{
  "name": "@earthstar-project/mini-rpc",
  "version": "10.0.0-beta.2",
  "description": "An RPC framework built in Typescript.",
  "author": "Cinnamon <cinnamon_npm@fastmail.com>",
  "homepage": "https://github.com/earthstar-project/mini-rpc",
  "license": "LGPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/earthstar-project/mini-rpc.git"
  },
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "scripts": {
    "clean": "rm -rf build coverage .nyc_output",
    "build": "tsc",
    "main": "VERBOSE=true node build/main.js",
    "test": "tap -Rspec --jobs=1 --no-check-coverage build/test/*.test.js",
    "test-coverage": "npm test -- --coverage-report=lcov",
    "clean-build-test": "npm run clean && npm run build && npm test",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.11.6",
    "@types/tap": "^15.0.5",
    "express": "^4.17.2",
    "tap": "^15.1.6",
    "typescript": "~4.5.4"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "concurrency-friends": "^5.2.0",
    "cross-fetch": "^3.1.4"
  }
}
